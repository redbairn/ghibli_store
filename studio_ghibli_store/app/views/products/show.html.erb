
  <p id="notice"><%= notice %></p>
  <div id="productsContainer">
    <h1><%= @product.title %></h1>
    <%= 
      # This document doesn't appear to render the one product like it should do.
      # After removing it form the views/products directory - there were no issues with the app.
      # It appears that this page will not show because of our little dot-not-slash issue
    
    image_tag @product.image_url, :class => 'welcomIm' %>
    <h2><%= @product.description %></h1>
    
    <div class="producthols">
      <p>Price <%= number_to_currency(@product.cost_price, :unit => "€") %>
      <br />
      Colour: <%= @product.colour %>
      <br>
      CategoryID: <%= @product.categories_id %>
      <br>
      SupplierID: <%= @product.supplier_id %></p>
      
      <p>
      <% if session[:login] == 1 %>    
        <%= link_to 'Edit', edit_product_path(@product), :class => "button", :role => "button" %>
      <% end %>
      <%= link_to 'Back', products_path, :class => "button", :role => "button" %>
    
      
      </p>
    </div><!-- End of producthols -->
   </div><!-- End of productsContainer -->


